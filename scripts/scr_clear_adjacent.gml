for(var i=0; i<4; i++) 
    if adjacent[i] && instance_exists(adjacent[i]) && adjacent[i].state = AtomState.Chosen1 
        then adjacent[i].state = AtomState.Idle;
adjacent_index = -1;   
obj_arrow.lookat = noone;
